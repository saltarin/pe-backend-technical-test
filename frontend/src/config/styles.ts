import { COLORS } from '../ui/colors';

export const STYLES = `

  html {
    font-family: Roboto;
    color: ${COLORS.BLACK};
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    min-height: 100vh;
  }

  ul {
    list-style: none;
  }
`;
